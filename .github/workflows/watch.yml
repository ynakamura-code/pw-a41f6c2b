name: platform-watch

on:
  schedule:
    # GitHub の無料の定期実行は混むと数時間後回しにされる。9:30〜12:00 の間に必ず1回終わらせたいので、
    # 40分おきに4回ぶん予約しておき、その日すでに実行済みなら後の回は何もせず終わる（下の「今日はもう実行したか調べる」）。
    # 毎時00分・30分ちょうどは世界中の実行が集中するため、半端な分にしている。
    - cron: "22 0 * * *"   # 毎日 9:22（日本時間）
    - cron: "7 1 * * *"    # 毎日 10:07
    - cron: "52 1 * * *"   # 毎日 10:52
    - cron: "37 2 * * *"   # 毎日 11:37
  workflow_dispatch:        # 手動でも実行できる

permissions:
  contents: write

concurrency:
  group: platform-watch
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 前回撮った画面写真を引き継ぐ（比較用）
      - uses: actions/cache@v4
        with:
          path: data
          key: pw-shots-${{ github.run_id }}
          restore-keys: pw-shots-

      - name: 今日はもう実行したか調べる
        id: guard
        run: |
          today=$(TZ=Asia/Tokyo date +%Y-%m-%d)
          last=$(cat data/last_run 2>/dev/null || echo "")
          if [ "$last" = "$today" ] && [ "${{ github.event_name }}" = "schedule" ]; then
            echo "本日（$today）は実行済みのため、今回は何もしません"
            echo "skip=yes" >> "$GITHUB_OUTPUT"
          else
            echo "skip=no" >> "$GITHUB_OUTPUT"
          fi

      - name: Chrome を用意する
        if: steps.guard.outputs.skip != 'yes'
        run: |
          if [ -x /usr/bin/google-chrome ]; then
            echo "PW_CHROME=/usr/bin/google-chrome" >> "$GITHUB_ENV"
          else
            sudo apt-get update -qq
            sudo apt-get install -y -qq chromium-browser
            echo "PW_CHROME=$(command -v chromium-browser || command -v chromium)" >> "$GITHUB_ENV"
          fi

      - name: チェックを実行する
        if: steps.guard.outputs.skip != 'yes'
        env:
          PW_SLACK_WEBHOOK: ${{ secrets.PW_SLACK_WEBHOOK }}
          PW_NOPUSH: "1"
        run: python3 watch.py

      - name: 結果を公開ページに反映する
        if: steps.guard.outputs.skip != 'yes'
        run: |
          TZ=Asia/Tokyo date +%Y-%m-%d > data/last_run
          git config user.name platform-watch
          git config user.email y.nakamura@busoken.com
          git add -A
          if git diff --cached --quiet; then
            echo "変わりがないため更新しませんでした"
          else
            git commit -qm "$(TZ=Asia/Tokyo date +%Y-%m-%d)"
            git push
          fi
